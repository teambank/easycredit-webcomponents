import { Meta } from "@storybook/blocks";

<Meta title="Getting Started/Changelog" />

# Changelog

## 1.3.5

* es wird nun `npm` statt `yarn` verwendet
* `stenciljs/sass` locked auf Version 3.0.12 wegen https://github.com/stenciljs/sass/issues/70.

## 1.3.4

#### `<easycredit-widget />`

* behebt ein Problem, das unter bestimmten Bedingungen zu einer Endlosschleife führen konnte.

## 1.3.3

* Upgrade auf StencilJS v4.27.0

#### `<easycredit-checkout />`, `<easycredit-express-button />`,

* das `submit`-Event wurde umbenannt zu `easycredit-submit` (Rückwärtskompatibilität bleibt erhalten)

#### `<easycredit-widget />`

* bei aktiver Zinsflex-Aktion zeigt das Widget ein Badge für Zinsen und Laufzeit (`disable-flexprice` deaktiviert das Badge und die Anzeige im Ratenrechner)

## 1.3.2

* Upgrade auf StencilJS v4.22.3

#### `<easycredit-widget />`

* das + Icon linkt nun immer auf die aktive Zahlungsart

#### `<easycredit-box-modal />`, `<easycredit-box-flash />`, `<easycredit-box-listing />`

* Austausch der Verlinkung

## 1.3.1

#### `<easycredit-widget />`

* die webshop-Id ist nun wieder dynamisch

#### `<easycredit-checkout />`

* `numberOfInstallments` wird nun mit `null` übergeben, wenn nicht gesetzt

## 1.3.0

* die Web-Komponenten wurden für easyCredit-Rechnung überarbeitet

## 1.2.10

#### `<easycredit-widget />`

* die Größe des Modals wurde an die neue Gestaltung des Ratenrechners angepasst

#### `<easycredit-checkout />`, `<easycredit-express-button />`

* der Error-Timeout wurde auf 10 Sekunden erhöht
* der Performance-Observer wurde entfernt, weil er durch die Same-Origin-Policy keine Wirkung hatte
=======

## 1.2.9

* Upgrade auf Storybook v8 & StencilJs v4.17
* das Tracking für `componentDidLoad` wurde entfernt

#### `<easycredit-widget />`

* es wurde eine neue Darstellungvariante `minimal` hinzugefügt

#### `<easycredit-checkout />`, `<easycredit-express-button />`

* das Fehlerverhalten der Komponente wurde verbessert
* im Fehlerfall sendet die Komponente nun Feedback zur Erkennung von Problemen

## 1.2.8

* Funktionen zum Tracking der Komponenten wurde hinzugefügt
* die Basis-URL für API-Aufrufe kann nun konfiguriert werden
* die globale Konfigurationsvariable wurde umbenannt zu `easycreditRatenkaufWebComponentsConfig`

## 1.2.7

* Upgrade auf Storybook v7 & StencilJs v4.0.4

#### `<easycredit-checkout />`, `<easycredit-express-button />`

* für das submit-Event werden nun JSX-Events statt des globalen Body-Listeners verwendet um doppelte Events zu vermeiden

#### `<easycredit-express-button />`

* der Express-Button wird nun nur noch innerhalb der Betragsgrenzen angezeigt, wenn das Attribut `amount` angegeben ist

## 1.2.6

#### `<easycredit-widget />`, `<easycredit-checkout />`

* die beiden Komponenten wurden um die Option `disable-flexprice` erweitert

## 1.2.5

* Update von StencilJs auf v3.1.0
* der Support für ältere Browser wurde aus der Dokumentation entfernt und wird in Zukunft vollständig entfernt werden (nomodule, deprecated)
* die Dokumentation wurde um die Komponenten-Events und Beispiele deren Nutzung erweitert

#### `<easycredit-modal />`

* das Modal wird automatisch auf Ebene des `body` verschoben um Seiteneffekte mit Sidebars o.Ä zu vermeiden

#### `<easycredit-box-listing />`

* die Listing Box passt sich nun automatisch an den verfügbaren Platz an und zeigt bei geringen Platzverhältnissen einen verkürzten Text an

#### `<easycredit-widget />`

* das Widget reagiert nicht mehr auf Klick-Events, um das Öffnen des Linkziels zu vermeiden, wenn es innerhalb eines Elements verwendet wird
* das Widget reagiert nun auf Betragsänderungen mit setAttribute, ein damit in Zusammenhang stehender Fehler wurde behoben

## 1.2.4

#### `<easycredit-modal />`, `<easycredit-box-flash />`, `<easycredit-box-listing  />`

* das Standard-Bild wurde gegen eine aktuellere Variante ausgetauscht

#### `<easycredit-widget />`

* das Widget wurde um das Attribute displayType erweitert
* der neue displayType="clean" wird ohne Schatten dargestellt

## 1.2.3

#### `<easycredit-checkout />`

* Bugfix: fehlende Styles wurden hinzugefügt

## 1.2.2

#### `<easycredit-express-button />`

* der Express-Button zur Implementierung des Express-Checkouts wurde hinzugefügt

#### `<easycredit-box-top />`

* der Kreis ist nun als SVG implementiert und wird in allen Browsern scharf angezeigt

#### `<easycredit-widget />`

* das Widget kann nun mit dem Attribut `extended` außerhalb der finanzierbaren Betragsgrenzen ausgeblendet werden
* das Widget reagiert nun auf Änderung des Attributs `amount` und berechnet die Rate automatisch neu
* der Text im Widget wird nun linksbündig dargestellt
* das Modal wurde farblich an den Ratenrechner angepasst

## 1.2.1

* die Basis-URL für Assets wird nun korrekt ermittelt
* der "Backdrop" des Modals erhält einen höheren z-index, um mit höherer Wahrscheinlichkeit andere Element zu überlagern

## 1.2.0

* alle Komponenten wurden im Rahmen der Markenumstellung grafisch und textlich angepasst auf "easyCredit-Ratenkauf"
* Update von StencilJs auf v2.17.1

#### `<easycredit-logo />`

* die Komponente wurde hinzugefügt

#### `<easycredit-checkout />`

* die Komponente reagiert nun auf das Event `closeModal`
* das Submit-Event wird nun über den ShadowDOM hinaus propagiert

#### `<easycredit-modal />`

* das Modal triggert das Event `modalClosed`, wenn es geschlossen wird

#### `<easycredit-merchant-manager />`

* das Verhalten des Formulars zum Melden der Lieferung / Rückabwicklung wurde optimiert
* die minimale Breite wurde reduziert

## 1.1.0

* Update von StencilJs auf v2.16.1

#### `<easycredit-checkout />`

* bei Klick auf Akzeptieren erhält der Nutzer visuelles Feedback
* die Auswahl der Anrede wurde entfernt
* die Zustimmungserklärung muss nun nicht mehr aktiv bestätigt werden (Checkbox entfernt)

#### `<easycredit-merchant-manager />`

* der Betrag wird mit zwei Dezimalstellen angezeigt
* Datumsangaben ohne Zeit werden nun als reines Datum formatiert dargestellt
* zwischen nicht verfügbar & noch nicht verfügbar wird nun korrekt unterschieden

#### `<easycredit-modal />`

* das Modal erhielt einen höheren z-index, damit es andere Elemente leichter überlagert

#### `<easycredit-widget />`, `<easycredit-merchant-manager />`, `<easycredit-merchant-status-widget />`

* die Komponenten  unterstützen nun automatisches Batch-Loading und können so bei guter Performance auch mehrfach innerhalb einer Seite eingesetzt werden.

### Upgrade Notes

* für das Batch-Loading muss in <em>window.ratenkaufbyeasycreditOrderManagementConfig</em> die Eigenschaft <em>list</em> mit entsprechendem Endpunkt gesetzt werden.

## 1.0.0

* erstes Release
